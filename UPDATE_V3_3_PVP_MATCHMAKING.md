# V3.3 — PVP Matchmaking ใช้ง่ายขึ้น

## รูปแบบใหม่

หน้า PVP มี 2 ปุ่มหลักเท่านั้น:

```text
[ ➕ สร้างห้อง ]
[ 🔎 ค้นหาห้อง ]
```

ไม่มีช่องให้ผู้เล่นตั้ง Room Code เอง

---

## 1. สร้างห้อง

เมื่อกด `สร้างห้อง`

ระบบจะ:

1. ตรวจภาษา/ด่านที่จะใช้แข่ง
2. สุ่ม Room Code 6 ตัว
3. ตรวจ Firestore ว่า Code ยังไม่ถูกใช้
4. ถ้าซ้ำ จะสุ่มใหม่อัตโนมัติ
5. สร้าง `pvp_rooms/{ROOM_CODE}`
6. แสดง Lobby และรอ Player 2

ตัวอย่าง:

```text
ROOM CODE
7KM3QX
```

ตัวอักษร `I`, `O` และตัวเลข `0`, `1` ถูกตัดออก
เพื่อไม่ให้อ่านสับสน

ผู้เล่นไม่สามารถแก้หรือกำหนด Code เองได้

---

## 2. ค้นหาห้อง

เมื่อกด `ค้นหาห้อง`

ระบบจะ:

1. ค้นหา Firestore collection `pvp_rooms`
2. เลือกเฉพาะ `status == waiting`
3. เลือกเฉพาะภาษาเดียวกับผู้เล่น
4. ตัดห้องเต็มออก
5. ตัดห้องของตัวเองออก
6. สุ่มลำดับห้อง
7. ใช้ Firestore Transaction เข้าห้อง

Transaction ช่วยลดปัญหา Player หลายคนเข้าช่อง Player 2
ของห้องเดียวกันพร้อมกัน

หากห้องถูกคนอื่นเข้าก่อน ระบบจะลองห้องถัดไป

---

## 3. ไม่พบห้อง

ระบบจะแจ้ง:

```text
ยังไม่พบห้องว่าง
```

ผู้เล่นสามารถ:
- กดค้นหาห้องอีกครั้ง
- หรือกดสร้างห้องใหม่

ระบบจะไม่สร้างห้องเองจากปุ่มค้นหา
เพราะยังคงแยกหน้าที่ของ 2 ปุ่มให้ชัดเจน

---

## 4. Lobby

เมื่อเข้าห้องแล้วจะแสดง:

- Room Code
- Player 1
- Player 2
- สถานะ
- ผู้เล่นครบ 1/2 หรือ 2/2

Host เท่านั้นที่เห็นปุ่ม:

```text
เริ่มการแข่งขัน
```

เมื่อผู้เล่นครบ 2 คน

มีปุ่ม:

```text
ออกจากห้อง
```

เพื่อออกจาก Lobby ก่อนเริ่ม

ถ้า Host ออกจากห้องตอนยัง Waiting
ระบบจะลบ Lobby นั้นออกจาก Firestore
เพื่อลดห้องร้าง

---

## 5. ไฟล์ที่ต้อง Replace บน GitHub

```text
index.html
app.js
style.css
```

Firebase Rules เดิมของ V3.2 ใช้ต่อได้
เพราะ `pvp_rooms` เปิด read/create/update สำหรับ User ที่ Login อยู่แล้ว

---

## 6. ไม่ต้องสร้าง Firestore Collection เอง

Collection:

```text
pvp_rooms
```

จะสร้างอัตโนมัติเมื่อ Player กด `สร้างห้อง`
